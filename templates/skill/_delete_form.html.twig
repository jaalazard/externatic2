<form method="post" action="{{ path('app_candidate_delete_skill', {'id': skill.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="btn btn-primary btn-sm fw-bold rounded-0">
    {% include 'icons/trash.svg' %}</button>
</form>