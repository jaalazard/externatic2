{% extends 'base-admin.html.twig' %}

{% block title %}JobOffer{% endblock %}

{% block body %}
    <h1>JobOffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jobOffer.id }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ jobOffer.job }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ jobOffer.city }}</td>
            </tr>
            <tr>
                <th>Contract</th>
                <td>{{ jobOffer.contract }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ jobOffer.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ jobOffer.createdAt ? jobOffer.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_jobOffer_index') }}">back to list</a>

    <a href="{{ path('admin_jobOffer_edit', {'id': jobOffer.id}) }}">edit</a>

    {{ include('admin_jobOffer/_delete_form.html.twig') }}
{% endblock %}
