{% extends 'base-admin.html.twig' %}

{% block title %}JobOffer{% endblock %}

{% block body %}
    <h1>JobOffer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_offer.id }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ job_offer.job }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ job_offer.city }}</td>
            </tr>
            <tr>
                <th>Contract</th>
                <td>{{ job_offer.contract }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_offer.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ job_offer.createdAt ? job_offer.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_job_offer_index') }}">back to list</a>

    <a href="{{ path('app_admin_job_offer_edit', {'id': job_offer.id}) }}">edit</a>

    {{ include('admin_job_offer/_delete_form.html.twig') }}
{% endblock %}
