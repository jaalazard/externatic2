<form method="post" action="{{ path('admin_jobOffer_delete', {'id': jobOffer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jobOffer.id) }}">
	<button class="btn">Delete</button>
</form>
