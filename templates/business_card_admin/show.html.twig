{% extends 'base.html.twig' %}

{% block title %}BusinessCard{% endblock %}

{% block body %}
    <h1>BusinessCard</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ business_card.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ business_card.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ business_card.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_business_card_index') }}">back to list</a>

    <a href="{{ path('app_business_card_edit', {'id': business_card.id}) }}">edit</a>

    {{ include('business_card/_delete_form.html.twig') }}
{% endblock %}
