{{ form_start(form) }}

<div class="errors">
	{{ form_errors(form) }}
</div>

<div class="form-row fw-bold">
	{{ form_row(form.etablissement) }}
</div>
<div class="form-row fw-bold">
	{{ form_row(form.diplome) }}
</div>
<div class="col form-row fw-bold">
	{{ form_row(form.start) }}
</div>

<div class="col form-row fw-bold">
	{{ form_row(form.end) }}
</div>
<div class="col form-row fw-bold">
	{{ form_row(form.description) }}
</div>
<div class="text-center">
	<button class="btn btn-primary w-50 w-md-25 fw-bold text-uppercase">Valider</button>
</div>
{{ form_end(form) }}
